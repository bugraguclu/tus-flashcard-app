/**
 * Anki's filtered-deck picker order. The stored values deliberately remain the
 * scheduler order ids used by the repository; only their presentation is reordered.
 */
export const FILTERED_DECK_ORDER_UI = [7, 1, 2, 3, 6, 4, 0, 5, 8, 9] as const;
