// ============================================================
// TUS Flashcard - Seed Data References
// ============================================================

import type { Subject, Card } from './types';

// The former demo curriculum was removed from the product. A fresh install starts empty, like
// Anki; catalog courses are registered when the purchased BKA package is installed, so no stale
// zero-count courses remain in navigation.
export const TUS_SUBJECTS: Subject[] = [];

export const TUS_CARDS: Card[] = [];
