// ============================================================
// TUS Flashcard - Seed Data References
// ============================================================

import type { Subject, Card } from './types';

// The former Python demo curriculum was removed from the product. BKA subjects are registered
// from the verified catalog during its one-time installation, so no stale zero-count courses
// remain in navigation.
export const TUS_SUBJECTS: Subject[] = [];

export const TUS_CARDS: Card[] = [];
