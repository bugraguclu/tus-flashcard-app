# Anki compatibility baseline

Last reviewed: 2026-09-04

TusAnkiM is an independent Anki alternative with local-first storage and iPhone as its sole
release target; web is only a local/CI regression target. “Compatible” here means a behaviour or interchange path has explicit code and tests; it
does not mean every Anki client feature is present. The canonical external references are listed
in `docs/anki-reference-sources.json`.

## Source hierarchy

Use the [Anki manual](https://docs.ankiweb.net/intro.html) for product semantics and the
[AnkiMobile manual](https://docs.ankimobile.net/) for iOS interactions. Resolve unclear low-level
behaviour against [Anki source](https://github.com/ankitects/anki). Use
[AnkiDroid source](https://github.com/ankidroid/Anki-Android) only for Android-specific behaviour.
The URLs supplied with a trailing `-` are not the canonical repositories; the links above are.

This MIT project must not copy GPL/AGPL implementation. Behavioural compatibility must be
independently implemented and protected by tests.

## Capability matrix

| Area | Status | Current evidence | Next compatibility gate |
| --- | --- | --- | --- |
| New/learning/relearning/review lifecycle | Implemented, legacy scheduler | `lib/scheduler.ts`, scheduler and repository tests | Keep golden interval/rollover tests |
| Answer buttons, next times, counts, undo | Implemented | opt-in redesigned reviewer with a preserved classic fallback, compact toolbar/footer, directional feedback, `lib/reviewerPresentation.test.ts`, review logger and repository tests | iPhone smoke in both classic and redesigned modes on each reviewer change |
| Daily limits, learning steps, lapses, leeches, burying | Implemented | deck options, Anki-style step parser, collection-limit and queue tests | Preserve selected-deck/subdeck semantics |
| Display/gather/sort order and learn-ahead | Implemented for Anki V3/SM-2 | six gather orders, five new-card sort orders, interday/review ordering and queue tests; the two FSRS-only retrievability orders are not offered locally but are preserved across an `.apkg` round trip instead of being rewritten to day (random) (`lib/exportReviewOrder.test.ts`) | Add FSRS-only retrievability orders with FSRS, not as inert controls |
| Audio, timers and auto advance deck options | Implemented | per-card preset resolution, reviewer timer/auto-advance behavior and package round-trip tests | Keep the study-session activation boundary explicit |
| Reviewer Timebox | Implemented | Anki-aligned wall-clock block, post-answer Continue/Finish checkpoint, per-block repetition count, `lib/timebox.test.ts` | Run the focused iPhone Timebox smoke when reviewer flow changes |
| FSRS | Implemented (FSRS-6), audited 2026-09-04 | `lib/fsrs.ts` pinned to upstream fsrs-rs vectors — including the published `next_states` golden vector, the FSRS-6 post-lapse ceiling and the short-term floor; `lib/fsrsScheduler.ts` state machine; review fuzz windows pinned against Anki's own fuzz table (`lib/fsrsScheduler.test.ts`); memory state in Anki's own `cards.data` (`s`/`d`/`dr`/`decay`); history-derived states and rescheduling whose fuzz floor and due anchor come from the revlog the way `get_last_revlog_info` does (`lib/fsrsMemory.ts`, `lib/fsrsMaintenance.ts`); parameter optimization (`lib/fsrsOptimizer.ts`); deck-options section, card info, `prop:s`/`prop:d`/`prop:r` search, browser sort columns, `.apkg` round trip that keeps an imported preset's retrievability review order. Full item-by-item verdicts in `docs/AUDIT_FSRS_ANKI_PARITY.md` | Known divergences from that audit: `Set Due Date` is not FSRS-aware (`lib/studyRepository.ts`); preview answers write no cramming revlog row; the fuzz seed is per study day rather than `card.id + reps`; the optimizer is not Anki's trainer and will not reproduce its numbers. Still to add: the FSRS-only review sort orders, retention/true-retention graphs, and a load balancer; the optimizer runs on the JS thread, so very large collections are sampled |
| Hierarchical decks and presets | Implemented | validated/atomic Deck Options save flow, local parent/subdeck scope picker, explicit deck-vs-preset labels, preset rename scoped to the selected config, hierarchical deck rename with subtree/collision protection, deck manager, deck-options scope/form, and deck-navigation tests | Continue parent/subdeck limit tests |
| Per-deck shortcuts | Implemented | deck overflow action, local `DeckShortcuts` module, Android pinned shortcut and iOS native Apple Shortcuts add flow | Android launcher approval/open smoke; iPhone Apple Shortcuts add/run smoke |
| Filtered decks and custom study | Implemented | Context-preserving overview panels, two independent filters, all 11 gather search orders (0..10) stored with Anki's own ordinals (migration 10), rescheduling/preview modes whose Again/Hard/Good delays mirror Anki's `preview_again_secs`/`preview_hard_secs`/`preview_good_secs` (read from `Deck.Filtered` fields 7/5/6, which `decks.proto` numbers out of order; an omitted field imports as zero exactly as proto3 and Anki's schema-11 serde defaults leave it, and `60`/`600`/`0` are used only when creating a deck, matching `Deck::new_filtered`, whose second search term also starts at 20 cards) with Easy always retiring the card as `preview_filter.rs` does, gather orders built from `rslib/src/storage/card/filtered.rs` including its note-id/ordinal added order and its single timeline for due order, reviewer session countdown timers, excluded-card reporting, in-app help, deck manager, `lib/filteredDeckOptions.test.ts`, `lib/studyRepository.scope.test.ts`, and reviewer preview queue tests | Retrievability ascending/descending and relative overdueness are approximated by overdue time relative to the last interval; Anki resolves all three through FSRS memory state in a registered SQL function, and answers the retrievability pair with no ordering at all when FSRS is off. A filtered deck is never written to an exported package and its cards return to their home deck; upstream instead exports it as filtered when every original deck is present, and otherwise converts it to a regular deck keeping the cards inside it |
| Custom study dialog | Implemented | Anki's six options with its spinner bounds, remembered limit deltas and per-deck include/exclude tags, the four cram kinds, and the per-option search/order/reschedule contract pinned by `lib/customStudy.test.ts` against `rslib/src/scheduler/filtered/custom_study.rs`; empty results and a name clash are refused the way Anki refuses them | Keep the spec test in step with upstream when Anki changes an option |
| Note types, conditional templates, cloze, typed answer | Implemented | AnkiMobile-aligned rich text editing toolbar grouped into Home/Styles/Insert tabs (Anki's own inline formats, lists, four paragraph alignments and indent/outdent as in `ts/routes/editor/editor-toolbar`; plus local additions Anki's editor does not have — block styles, quote, code block, table, validated http/https/mailto link, callout and font size — with MathJax and a raw HTML source editor), whose insertions are pinned by `lib/editorToolbar.test.ts` to pass the field sanitizer unchanged, note-type template aware card generation validation (a note saves whenever any template would generate a card, so an empty front is allowed when the reversed card generates — a deliberate divergence: Anki's `note_fields_check` tests only whether the first field is blank and refuses the note either way), native or template-positioned `#typeans` input, optional focus, token-bound/bounded WebView bridge, template, bridge, settings round-trip and note manager tests; dynamic note-type picker exposing all collection note types with field/template counts and `/note-types` management shortcut; dynamic multi-field editor rendering arbitrary N-fields with per-field sticky pinning (`lib/editorStickyFields.ts`), individual media attachment, localized labels and RTL/font styles; and real-time first-field duplicate detection (`findDuplicateNote`) matching AnkiMobile non-blocking warning badge with target deck reporting (`lib/editorDynamicFields.test.ts`) | Validate more add-on filters and complex templates |
| HTML, images, audio, video, TTS | Implemented subset with an untrusted-content boundary | sanitizer/CSP, card-content, media and package tests; Anki TTS language/voice/speed parsing, installed Enhanced/Premium iOS voice preference, generation-safe single speech queue, reviewer-only focus/foreground gating and hidden-content filtering are covered by focused TTS tests; scripts, network loads, active media documents and silent navigation are blocked. Reviewer and note editor both resolve Anki's bare media filenames by loading their WebView from the media directory (`lib/localMediaDocument.ts`), so a field shows its own images while its stored HTML keeps the plain filename; both documents refuse every navigation but their own base, keep file-origin reads and network off, and are pinned by `lib/localMediaDocument.test.ts` and `lib/cardContentSecurity.test.ts`. An attached file is named after the bytes actually copied rather than after the picker's label for them (`mediaFilenameForPickedAsset`, `lib/mediaFilename.test.ts`): iOS reports the library original (`IMG_0042.HEIC`) while handing over a transcoded JPEG, and storing that name would put a mislabelled media entry into the collection and into every `.apkg` exported from it; the same rule names an untouched photo saved out of the editor. The reference written into the field is built in one place and escaped for the attribute it lands in (`mediaReferenceSnippet`, `lib/mediaAttach.test.ts`), so a quote or an ampersand in a picked filename cannot close `src` early, and a bracket is taken out of an audio filename before the file is stored rather than truncating its own `[sound:…]` marker | Add offline MathJax and custom-font runtime QA without weakening CSP; web still leaves field media unresolved because its media lives in IndexedDB. A non-media file attachment is stored, referenced and exported, but the link policy allows `href` only for a fragment or an absolute https URL, so the reviewer renders it with `href="#"` and it does not open the file (pinned in `lib/mediaAttach.test.ts`); opening one needs the link policy and the reviewer's navigation rules changed together |
| MathJax/LaTeX runtime rendering | Not implemented | Source fields are preserved on import | Bundle an offline renderer; never depend on a CDN |
| Whiteboard/scratchpad and drawing attachment | Implemented subset | geometry/photo tests, reviewer UI, and a blank drawing page that opens the full annotation editor (pen, highlighter, shapes, movable text, partial/whole eraser, undo/redo, crop, rotate) with ruled/plain paper pinned by `lib/blankCanvas.test.ts`; the saved PNG is attached as a normal `<img>` media reference. A page carries its ruling rather than re-deriving it, so cropping trims the sheet without resizing its squares and a quarter turn takes the rules with the ink — writing stays on the line it was written on, and lined paper turned on its side is ruled down the page; undo restores the sheet along with the strokes. The paper, page colour and shape a drawing was made on are remembered and reopened for the next one (`lib/blankCanvasSetup.ts`), a local row like the board's, and leaving the editor with unsaved ink asks first. The PNG is rendered on its own off-screen surface at the drawing's real resolution rather than photographed off the canvas the user drew on, so a page created at 1600×1200 is exported at 1600×1200 and an annotated photo keeps its detail up to a 2000px cap instead of collapsing to a screen's worth of pixels; stroke widths and label sizes are taken up with the surface, so the exported drawing is the one that was on screen (`photoExportSurface`, `scalePhotoAnnotation`, `lib/photoEditor.test.ts`). The reviewer board is bounded to the card stage, so ink never lands on the reviewer toolbar or the fixed answer bar, and those stay tappable while drawing. Ink clears only on a genuine move to another card (as AnkiDroid does), on a deck switch, or when the learner clears it: an empty queue is not a card change, so a learn-ahead rebuild, a timebox checkpoint or the "all done" screen no longer destroys a drawing. Auto Advance is suspended while the board is open and its dwell resumes rather than restarting, and a background queue refresh holds the drawn-on card in front; both rules are pinned by `lib/reviewerTimers.test.ts` The board's on/off state, stylus-only choice and pen colour are stored per deck and restored on return, as AnkiDroid's MetaDB `whiteboardState` row does, with a separate pen kept for the light and dark themes and non-hex colours refused before they reach the renderer (`lib/whiteboardSession.ts`, `lib/whiteboardSession.test.ts`). Like MetaDB, the row is local and stays out of the exported collection: a pen colour describes the device in the learner's hand, not the collection | Apple Pencil-only mode is pending. In the classic (default) study layout the answer buttons scroll inside the card column, so they sit under the board while drawing is on; AnkiDroid keeps them in a separate bar |
| Browser, card/note table modes, tags, flags, marks, suspend/bury, reposition | Implemented subset | In-place searchable deck scope picker with inline create-and-select, scoped tag and multi-flag filters, optional safe audio-filename projection, Notes-mode first-card and aggregate-row semantics, browser/search/table-mode/selection tests | Expand full Anki search grammar and flag naming |
| Empty Cards maintenance | Implemented subset | Canonical `/empty-cards` full-screen route; reports cards whose generated front/cloze/template is no longer valid, deletes cards without deleting notes or sibling cards, uses explicit destructive confirmation and transactional bulk deletion | Add a named iPhone smoke test for VoiceOver, scan failure and destructive deletion |
| `.apkg` import/export with media and scheduling | Implemented subset with bounded archive and SQLite validation | package round-trip, backup-source export, archive-security, SQLite-security and import integration tests; a stored snapshot can be exported without consulting or replacing the live collection, known file size is checked before reading, and every package import creates a recovery backup | Maintain fixtures from current Anki releases and physical-device large-package smoke |
| `.colpkg` replacement | Implemented with explicit destructive confirmation and pre-operation safety backup | import confirmation and backup tests | Add real-device large-package smoke |
| CSV/TSV/TXT import/export | Implemented; Anki's own option set (separator with override, Allow HTML in fields, note type, deck, Existing notes, Match scope, Tag all notes, Tag updated notes, per-column field mapping) and its per-note outcome buckets. Fields are escaped exactly as Anki's importer does when HTML is off (minimal entity set plus newline → `<br>`), an unchanged match is reported as "already present" instead of being rewritten, a `#notetype column` naming an unknown type skips the row rather than filing it elsewhere, and a plain-text export with HTML excluded reduces fields to text so the `#html:false` header it writes is true. Update preview/confirmation and backup protect matching notes, and deck directives share the note-write transaction | `lib/importNotes.test.ts` (escaping, buckets, tag options, note-type resolution), `lib/importLog.test.ts`, `lib/importDelimited.test.ts`, `lib/exportNotes.test.ts`, delimited preview/import/export and rollback tests | Keep metadata-header fixtures; the mapping table draws at most 24 columns |
| Import screen and Import Log | Implemented | `/import` is file-first like Anki's: the extension chooses the importer and the option list that follows, and every import ends on an Import Log that files each note into Anki's own buckets (added, updated, already present, first field matched, note type changed, note type missing, deck missing, empty first field) with an expandable sample per bucket. Package imports add Anki's learning-progress, deck-preset and update-notes/update-note-types choices. `lib/importLog.test.ts`, `lib/importFile.test.ts` | "Merge note types" is not offered: a schema-conflicting note type is imported as a separate copy instead, so no inert control is shown. Add an iPhone smoke test for the mapping table and the log |
| iOS Files “Open in” hand-off | Implemented | document UTI config and `importFile` tests; `.zip` is accepted as a package the way Anki's own file dialog does | Verify once on a physical iPhone per release |
| Local automatic/manual backup and restore | Implemented with strict row validation | backup-validation, backup-source export, backup and storage round-trip tests; Share opens the canonical export workflow over the selected snapshot | Verify export options, Files share destination and plaintext warning per release |
| Collection/database and media check | Implemented subset | Database: the audit and the repair share one definition of every defect (`lib/maintenance.ts`), so "Onar ve optimize et" cannot claim a fix the read-only "Veritabanını kontrol et" does not see. The button audits first, shows the counts it found, and repairs only after the learner accepts; `lib/maintenanceWorkflow.ts` puts a safety backup on disk before any row is rewritten. The repair runs as one transaction, deletes cards no note owns and notes with no cards left, graves what it deletes, rescues cards whose deck is gone (home deck first, otherwise "Kurtarılan Kartlar"), and never touches a paid-catalog row; then REINDEX, ANALYZE and the search rebuild run with VACUUM last, and any step that fails is named instead of collapsing into one blanket error (`lib/maintenance.test.ts` against real SQLite, `lib/maintenanceWorkflow.test.ts`, `lib/noteManager.searchIndex.test.ts`). Media: deterministic media-reference audit tests; one scanner answers what a field, template or stylesheet refers to, and the audit, the `.apkg` exporter and the import rewriter all ask it (`extractMediaFilenames`, `rewriteMediaReferences`, `lib/mediaAttach.test.ts`). It reads `[sound:…]`, `src`/`href`/`poster` quoted or not, and `url(…)`, and decodes the entities an attribute is written with. They used to be three patterns that disagreed: the audit did not read a note type's CSS or templates and so called a card background unused, the exporter did not read `<a href>` and so shipped a package without the file it referenced, and the import rewriter knew about neither and left a renamed file referenced under its old name | Anki additionally regenerates cards a note type's templates should produce, deletes notes whose note type is gone, and clamps out-of-range card properties; none of those are repaired here. Its tag-list rebuild has no counterpart because tags are derived from the notes rather than kept in a registry. A note whose JSON no longer parses is reported and left alone — its text can only come back from a backup. Revlog rows belonging to a deleted card are kept on purpose, so lifetime statistics do not shrink. Add a named iPhone smoke test for the audit → confirm → repair flow |
| iPhone reviewer preferences and external automation | Implemented subset | persisted classic/redesigned reviewer opt-in; AnkiMobile-aligned, separately configurable question/answer 3×3 tap zones with assignable Add note action; interactive card-element pass-through; adjustable swipe sensitivity and edge-only vertical swipes; independent Flag/Tools surfaces; `lib/reviewerTouchControls.test.ts`, `lib/reviewerAddNote.test.ts`, `lib/reviewerSurface.test.ts`, storage round-trip, strict HTTPS/Shortcuts callback parsing and media-audit tests | Add a native Share extension before broader parity claims |
| iPhone study notifications | Implemented | AnkiMobile daily review reminder, AnkiDroid-compatible 0/10/25/50/75/100/150/200/500 due-review thresholds, default-on provisional iOS authorization, no badge, notification policy/content tests | Verify provisional and fully authorized delivery on a physical iPhone; vibration and LED/flash remain system-owned iOS settings |
| Statistics and card info | Implemented subset | Anki-aligned 12-month default; Today, Future Due, Reviews/Review Time, Answer Buttons, Review Intervals, Card Counts and Added views; pinned chart readouts, accessible summaries, stats presentation/axis/review logger tests | Add Calendar, True Retention and FSRS-specific views with reference fixtures before a full-parity claim |
| AnkiWeb synchronization | Not implemented | Local-only architecture | Requires a documented protocol/backend, conflict model, encryption and recovery plan |
| Profiles | Not implemented | Single local collection; no inert profile toggle is exposed | Design isolation, migration, backup and recovery before UI |
| Shared-deck marketplace | Product-specific catalog only | BKA catalog tests. The protection is enforced where the rows are written, not by hiding buttons: `assertCatalogCardsMovable`, `assertCatalogNoteContentMutable` and `assertCatalogDeckNotDeletable` are called from `lib/noteManager.ts` and `lib/deckManager.ts` themselves, and the export functions re-check independently of the picker that filters catalog decks out. What the catalog owns is its content — the fields, the note type, the guid, the deck it lives in. What the learner owns is their own study metadata, so flags carry no assertion at all and tags are written through `setNoteTags`, a path that re-reads the stored row and swaps only the tag list, on the trial tier as well as the full one. A selection mixing personal and catalog notes tags all of them rather than rolling the batch back (`lib/catalogProtection.test.ts`, `lib/noteManager.catalogTags.test.ts`) | Do not present it as AnkiWeb shared decks |
| Image Occlusion note authoring | Not implemented | — | Add a native/offline editor and package fixtures |

## Priority roadmap

1. **Protect the collection.** Keep transactional imports, pre-restore/pre-replacement backups,
   catalog ownership isolation, and round-trip tests green.
2. **Finish iPhone boundaries.** Files hand-off, notifications, purchases, keyboard/safe-area
   behaviour, VoiceOver labels, Dynamic Type review, and recoverable error states.
3. **Add FSRS deliberately.** Treat FSRS as a versioned scheduling engine, not a settings toggle.
   Ship only with official reference vectors, existing-history migration tests, preview parity,
   and an automatic backup before rescheduling.
4. **Close rendering gaps.** Offline MathJax, custom fonts, more template filters, and image
   occlusion come before broader visual polish.
5. **Design sync as a data system.** Offline study must remain authoritative and usable; specify
   tombstones, conflicts, media hashes, full-sync recovery, authentication, and observability
   before implementing a sync button.

## Definition of done for an Anki-facing change

- The exact manual/source page and intended behaviour are identified.
- Compatibility and licensing boundaries are stated in the change.
- Pure logic has deterministic tests; database changes have round-trip or migration tests.
- The matrix status and evidence are updated.
- `npm run quality` passes.
- Only the relevant iPhone smoke path is run and recorded; no blanket manual regression is
  required for a pure-logic change.
