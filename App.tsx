import React from 'react';

// Expo Router is configured as the runtime entrypoint via package.json.
// This file intentionally stays minimal to avoid stale placeholder UI.
export default function App() {
  return null;
}
